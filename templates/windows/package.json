{
  "name": "{{APP_NAME_SAFE}}",
  "version": "1.0.0",
  "description": "{{APP_NAME}} Windows Application",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "{{PACKAGE_ID}}",
    "productName": "{{APP_NAME}}",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "portable",
      "icon": "src/icon.ico"
    }
  }
}
